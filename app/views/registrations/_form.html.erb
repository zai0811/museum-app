<%= form_with(model: registration) do |form| %>
  <% if registration.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(registration.errors.count, "error") %> prohibited this registration from being saved:</h2>

      <ul>
        <% registration.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :manager_email, style: "display: block" %>
    <%= form.text_field :manager_email %>
  </div>

  <div>
    <%= form.label :museum_name, style: "display: block" %>
    <%= form.text_field :museum_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
